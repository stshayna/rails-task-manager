<h1><%= @task.title %></h1>
<p><%= @task.details %></p>
<% status = @task.completed ? "☑ This task is completed!" : "☐ This task is not completed yet." %>
<p><%= status %></p>
<p></p>
<%= link_to "Edit this task", "/tasks/#{@task.id}/edit" %>
<%= link_to "Back to list", tasks_path %>
